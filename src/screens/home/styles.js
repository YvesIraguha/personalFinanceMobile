import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    margin: 30
  },
  monthText: {
    fontSize: 18,
    marginBottom: 30,
    lineHeight: 20
  }
});

export default styles;
